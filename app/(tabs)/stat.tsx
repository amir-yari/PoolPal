import { ScrollView, Text, View } from "react-native";

const Tab = () => {
  return (
    <View>
      <ScrollView>
        <Text>Tab [Home|Settings]</Text>
      </ScrollView>
    </View>
  );
};

export default Tab;
